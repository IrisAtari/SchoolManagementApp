<Window x:Class="SchoolManagementApp.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SchoolManagementApp"
        xmlns:MaterialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Style="{StaticResource MaterialDesignWindow}"
        TextOptions.TextRenderingMode="Auto"
        WindowStartupLocation="CenterScreen"
        Title="Đăng Nhập" Width="450" Height="325">
    <Window.Resources>
        
    </Window.Resources>
    <Grid>
        <MaterialDesign:Card Width="425" Height="275"  HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid>
                <Grid.Resources>
                    <Style TargetType="Grid">
                        <Setter Property="Margin" Value="10 10 10 0"></Setter>
                    </Style>
                </Grid.Resources>
                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Label FontSize="36" Content="Quản Lý Giảng Đường" HorizontalAlignment="Center" VerticalAlignment="Top" Height="80"/>
                </Grid>
                <Grid Grid.Row="1">
                    <TextBox
                        MaterialDesign:HintAssist.Hint="Tên Tài Khoản"
                        AcceptsReturn="True"
                        Style="{StaticResource MaterialDesignFilledTextBox}"
                        TextWrapping="Wrap"
                        x:Name="txtBoxAccName"/>
                </Grid>
                <Grid Grid.Row="2">
                    <PasswordBox
                        MaterialDesign:HintAssist.Hint="Mật Khẩu"
                        Style="{StaticResource MaterialDesignFilledPasswordBox}"
                        x:Name="txtBoxPassword"/>
                </Grid>
                <Grid Grid.Row="3">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition></ColumnDefinition>
                        <ColumnDefinition></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" x:Name="btnDangKy" FontSize="20" 
                            Content="Đăng Ký" Height="40" Click="btnDangKy_Click"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Width="150"
                            HorizontalAlignment="Left"
                            />
                    <Button Grid.Column="1"  x:Name="btnDangNhap" FontSize="20"
                            Content="Đăng Nhập" Height="40" Click="btnDangNhap_Click"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Width="150"
                            HorizontalAlignment="Right"
                            />
                </Grid>
            </Grid>
        </MaterialDesign:Card>
    </Grid>
</Window>
